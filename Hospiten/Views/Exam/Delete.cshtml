@using Hospiten.Core.Application.ViewModel.Exam;
@model SaveExamViewModel
@{
    ViewData["Title"] = "Elimianr Examen";
}

<div class="row">

    <div class="col-6 offset-3">
        <div class="card">
            <div class="card-header bg-danger text-light">
                <h4>Esta seguro que desea eliminar el Examen @Model.Exam_Name?</h4>
            </div>
            <div class="card-body">

                <form method="post" asp-controller="Exam" asp-action="DeletePost">

                    <input type="hidden" asp-for="Exam_Id" />

                    <button type="submit" class="btn btn-danger">Eliminar</button>
                    <a asp-controller="Doctor" asp-action="Index" class="btn btn-warning">Cancelar</a>

                </form>

            </div>
        </div>
    </div>

</div>

