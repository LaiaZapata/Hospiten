@using Hospiten.Core.Application.ViewModel.Doctor;
@model SaveDoctorViewModel
@{
    ViewData["Title"] = "Elimianr producto";
}

<div class="row">

    <div class="col-6 offset-3">
        <div class="card">
            <div class="card-header bg-danger text-light">
                <h4>Esta seguro que desea eliminar el Paciente @Model.Name?</h4>
            </div>
            <div class="card-body">

                <form method="post" asp-controller="Doctor" asp-action="DeletePost">

                    <input type="hidden" asp-for="Doctor_Id" />

                    <button type="submit" class="btn btn-danger">Eliminar</button>
                    <a asp-controller="Doctor" asp-action="Index" class="btn btn-warning">Cancelar</a>

                </form>

            </div>
        </div>
    </div>

</div>

